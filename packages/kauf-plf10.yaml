# Default PLF10 entrypoint (kauf profile + plf10 target).
substitutions:
  component_family: kauf
  component_source: release
  profile:          default

packages:
  Kauf.Profile: !include profiles/${component_family}-${profile}.yaml
  Kauf.Target:  !include targets/${component_family}-plf10-target.yaml
